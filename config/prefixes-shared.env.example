# Shared origin hostname prefixes (same for every client on this VM).
# One label per line. Resulting hostname: <prefix>.<current-domain>
#
# EGT "Operator Proxy Server Setup Guide", Group 2. player-history and
# tournaments currently route only via their -bgsp form; both are listed so the
# non-bgsp names keep working once EGT complete their migration.

lottery-api-instant
lottery-api-instant-prod
lottery-web-prod
replays-ong-prod-ext

player-history-prod-bgsp
tournaments-prod-bgsp

player-history-prod
tournaments-prod
